import React from "react";

const Checkbox = () => {
  return (
    <div className="flex items-center gap-4">
      Select:
      <input type="checkbox" />
    </div>
  );
};

export default Checkbox;
